# Server Configuration
PORT=4000
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000

# Supabase Configuration (Optional: If left blank, will use the built-in resilient in-memory / JSON store with complete seed data)
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Storage
STATIC_BASE_URL=http://localhost:4000/static
